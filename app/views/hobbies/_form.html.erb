<%= form_with(model: hobby, class: "card card-body shadow-sm") do |form| %>
  <% if hobby.errors.any? %>
    <div class="alert alert-danger">
      <h2 class="h6 mb-2"><%= pluralize(hobby.errors.count, "error") %> prohibited this hobby from being saved:</h2>

      <ul class="mb-0">
        <% hobby.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
